# Workspace assets

Store durable workflow inputs and outputs here when they belong with the repository: approved prompt templates, sanitized fixtures, diagrams, exported reports, or evidence manifests.

Commit intentional reusable assets. Put transient or machine-generated artifacts under `generated/`; that directory is ignored by Git. Never store credentials, raw private webhook payloads, or unrestricted production logs here.
