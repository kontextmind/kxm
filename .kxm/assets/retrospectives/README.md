# Workflow retrospectives

`pi-mesh retrospective export` writes bounded Markdown and JSON here.

Exported files are proposals. Review them before changing workflow policy, gates, or harness configuration. Do not commit live run exports that may contain private engineering context.
