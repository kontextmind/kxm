# Workspace state

The default hub database is `kxm.db` in this directory. SQLite sidecars and other restart-recovery files also stay here.

Runtime state is ignored by Git. Back it up and restore it according to the operations guide; do not treat Git as a database backup mechanism.
