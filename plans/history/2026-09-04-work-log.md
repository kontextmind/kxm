# Work log

Append entries here. Format: unit, date, PR, landed, surprise, next.

- **B0** 2026-09-04, PR #105 (merged 08:31Z). Landed: CLI tests for update-notice paths, kxm-update-config split, dist rebuild; coverage 95.23/83.21/94.81. Surprise: kxm-update.ts is outside the include list so only cli.ts counted; local coverage without typebox omits extension.ts and looks worse than CI; the "punt wiki" docs commit was never pushed and rode in with this squash. Next: B1 inverts the include list; A4 replaces the PATH-emptying installer test with injected spawn.
- **A1** 2026-09-04
  PR pending (closes #78).
  Landed: first-run six-step path; deleted `kxm mesh`; skill `kxm_*` plus D6 marker; dash keys from TUI help; MCP name `kxm`; agents.json project `kxm`.
  Surprise: `kxm mesh init` still seeded isolated-workspace CLI and packed-install tests; those now mkdir or call `hub help`.
  Next: A3 implements `kxm hub bind` (docs already name it); A2 leftover Mesh strings; D6 peer/workflow CLI.
- **A2** 2026-09-04
  PR pending (closes #79).
  Landed: `kxm --version`; `schema: kxm.cli-result.v1` on every CLI payload (worker envelopes keep theirs); `ok:false` to stderr in both modes; `runs status|cancel|list` labels; `kxm run` Phase 3a notice plus `phase: "pre-3a"`; `hostMode` is `hub`; headers `x-kxm-agent-id|key|delivery-id|events-mode`; caller id `kxm-admin`; Pi custom types `kxm-inbound|recovery`; bin script and extension messages say KXM; dead `mesh_` prefixes in diagnostics now `kxm_`.
  Surprise: stderr routing touched 60 test assertions across seven files (a script flipped reads after non-zero exits, then five stragglers by hand); `--workspace` on `init` was already rejected. The literal gate "grep mesh in src returns only Prometheus names" does not fit the 600-line budget: 300 hits remain, all internal identifiers (`MeshClient`, `createMeshHub`, `MeshStore`, `runMeshTui`) and persisted schema tags (`pi-mesh.workflow-message-context.v1`, `pi-mesh.verified-peer-evidence.v1`, `pi-mesh.workflow-degradation-approval.v1`, `pi-mesh.retrospective.v1`). Split: identifiers go with B4's package seams; schema tags go with the D-unit that redefines each record. Also fixed on the A1 branch: Windows CI flake in the SIGKILL recovery test (rmSync EPERM on teardown, now retried).
  Next: A3 (`kxm hub bind`, hub start hygiene) and A4 (update safety) do not overlap A2's files except `cli.ts`; B4 should carry the identifier rename; E3 renames `pi_mesh_*` metrics.
- **A3** 2026-09-04
  PR pending (closes #80). Planned headless by Fable, written headless by Grok (`grok` CLI, 15.6 min, $0.50).
  Landed: `kxm hub bind <url>` / `kxm hub unbind` writing host-level `<KXM_STATE_HOME>/hub-binding.json` (`kxm.hub-binding.v1`, hand-validated) with a 300 ms `/health` probe reporting on/off/unknown; `runtimeFrom` resolves `KXM_SERVER_URL` → bound URL → default; deleted `init --hub|--hub-url` and `hub-setup.ts`; hub start is cache-first with a background refresh and warns instead of exiting 2 on a malformed `.kxm/update.yaml`; startup line carries `auth=token|none`; pid-claim failure prints one line pointing at `kxm hub stop`. 432 tests, 431 pass, 1 skip.
  Surprise: the unit said "Runtime-local bindings", but that record is project-scoped, schema-closed (`additionalProperties: false`, `minProperties: 1`) and Git-locked, so a host-level `hub bind` outside a project would have failed — hence a separate file, `vnext-bindings.ts` untouched. `hub-setup.ts` took an `online` flag no caller ever passed, so `init --hub existing` always printed "not probed". `check:generated` diffs `dist` against `HEAD`, so it cannot go green on uncommitted work — a local commit was required; B1 owns that gate and should know. CLI tests now default `KXM_STATE_HOME` to a temp dir so a developer's real host binding cannot leak into them.
  Next: non-loopback `http` binds are still allowed, so a token would go over cleartext — left for a post-MVP ops unit, deliberately not in A3. A4 (update safety) does not overlap these files except `cli.ts`.
- **B1** 2026-09-04
  PR pending (closes #82). Planned headless by Fable, written headless by Grok.
  Landed: coverage include inverted to `plugins/kxm/src/**/*.ts` (note `**/*.ts`, not the unit's `**.ts` — a `**` not followed by `/` matches one segment and would have silently dropped `context/providers.ts`); only two excludes, `server.ts` and `mcp-server.ts`, both because they execute solely as spawned bundles so coverage attributes to `dist`. Measured 93.75/80.87/93.53, thresholds set 93/80/93; independently re-measured 93.79/80.84/93.53. Ratchet rule in `CONTRIBUTING.md`. `verify` now ends with `check:generated`, which compares built `dist` to the **staged** copy rather than HEAD. `kxm-hub`/`kxm-worker` bins dropped, peers optional and pinned as devDeps, `provenance-quorum.json` ships.
  Surprise: lines *drops* 95 → 93 while the gate gets much stronger — the old 95 measured 13 hand-listed files, the new 93 measures all 42 non-excluded ones, and functions rises 90 → 93. Said so explicitly in the CHANGELOG or it reads as a weakened gate. `check:generated` had to move from `git status --porcelain` (staged **and** unstaged vs HEAD) to a diff against the index, or putting it in `verify` turns the commit gate into a post-commit gate. Fable's plan cited a nonexistent `docs/vnext/decisions.md`; the decisions live in `.kxm/assets/reviews/`. `verify` still does **not** enforce coverage (`npm test` is uninstrumented) — only CI's `validate:ci` does, so a clean local gate can still fail CI on coverage.
  Next: branches has only 0.84 of margin and wobbles ~0.05 between runs; confirm the four CI all-files rows before any ratchet raise. Unloaded files are invisible to Node's coverage, so add an import-every-non-excluded-module test before the next raise. B2 can drop the now-redundant standalone `generated` job together with a `protect-main` ruleset edit.
- **A4** 2026-09-04
  Superseded by PR #114 continuation below (closes #81). Planned headless by Fable, written headless by Grok.
  Landed: `auto` is read only from `<userStateRoot>/update.yaml`, never the working directory — a cloned repo shipping `auto: true` could previously make a bare `kxm update` install (`cli.ts` `applyKxm = options.kxm || notice.auto` plus `loadKxmUpdateConfig(runtime.cwd)`); a project `.kxm/update.yaml` now warns on stderr and is ignored. Install-kind detection (npm-global, pi-git, claude-marketplace, source, unknown) fails closed with a per-kind instruction. A release asset digest is required before `npm install`; absent digest is fatal (`release_digest_missing`), same as a mismatch. Ships `.kxm/config/update.example.yaml`. Replaced B0's PATH-emptying installer test with injected spawn. 441 tests at that snapshot; see 2026-09-05 entry for the gate that ships.
  Surprise: the Pi git install at `~/.pi/agent/git/...` is a full clone with `.git/`, so the unit's "Git root containing this package's package.json" rule alone would have misclassified it as a source checkout — detection order matters. An `npm link`ed global resolves through the symlink and correctly classifies as source. `--check` reports `installKind: npm-package` rather than `npm-global` because `npm root -g` is only spawned on the apply path.
  Next: **B2 must upload the asset as `kxm-<v>.tgz`**, not npm pack's default `kontextmind-kxm-<v>.tgz`, add a test importing `kxmReleaseAssetName` against `release.yml`, and confirm `gh release view --json assets` shows a `digest`. Until then a real npm-global `kxm update --kxm` stops at `release_digest_missing`, which is the correct fail-closed state. No sidecar `.sha256` in A4.
- **A4** 2026-09-05
  PR #114 (closes #81). Continuation after merging origin/main `94c01a9` (PR #117) `--no-commit` into `a4-update-safety` `eeb663d`, then Codex review rework of `--kxm` kind-check ordering.
  Landed: `isUnder` compares path segments so synthetic win32 probes work on POSIX; Tracking keeps A4 updater wording plus PR117 mesh/session brakes; B2 release-asset remains first Still-open. Explicit `--kxm` resolves install kind whenever apply is requested, so pi-git / marketplace / npm-local / unknown refuse with `install_kind_*` even when already current or the release check is unavailable; auto-apply still prints the non-global hint only when an update is available; `--check` still does not spawn `npm root -g`. Historical `npm run verify` before this review fix: 446 tests, 445 pass, 1 skip (artifact `verify.log`). Final rework gate results are in the ignored artifact `rework-verify.log` under `.kxm/logs/phase-loop-20260905/a4/`; this log does not claim that run passed.
  Surprise: Linux CI failed only `test/kxm-install-kind.test.ts:75` (`unknown` vs `pi-git`) because `isUnder` used the host `sep`; Windows passed. First harness verify failed 4 spawn tests on Node `NO_COLOR`/`FORCE_COLOR` stderr (not product); command-level `env -u NO_COLOR -u FORCE_COLOR` is the fix, not helper or expectation changes. Merge conflict was only `docs/vnext/implementation-plan.md`. Codex found explicit `--kxm` skipped refusal when current or offline because kind resolution was gated on `notice.available`; the Fable review passed the scoped delta and missed it because those updater invariants were out of that review's scope. review-sol's "still running" claim was a timing assumption: the earlier verify had completed, but the work log was edited after that run, so the gate had to rerun.
  Next: independent Fable + Codex review of this ordering fix (artifacts, not hub peer-reply); root ships. B2 must emit `kxm-<v>.tgz` with a GitHub digest. Issue 115 untouched. Regression tests hold the availability-independent `--kxm` refusal; no extra review checklist.
- **B2** 2026-09-05
  Branch `b2-release-ci` (closes #83 later). Planned by Fable (`plan.md` +
  `plan-refine.md`), written by Grok in `/tmp/kxm-b2-loop`. Fable+Sol PASS at
  `80b1981`.
  Landed: `release.yml` on `v*` asserts tag==package version before `npm ci`,
  runs `validate:ci` + `check:generated`, packs `kxm-<v>.tgz`, fail-closed
  draft upload via `scripts/kxm-release-github.mjs` (404-only create; published
  / auth / 5xx / digest mismatch never mutate; matching digest is idempotent
  skip; listed REST digest is the proof). Dropped standalone `generated` job;
  `check:generated` stays on every validate leg. Hosted `Plugin validation`
  with `@anthropic-ai/claude-code@2.1.261`. PR template: slice issue + verify;
  classify path is lowercase `pull_request_template.md`. npm publish job
  `if: false`. Coverage flags untouched. `v0.5.1` untouched.
  Surprise: older issue wanted six required checks and a generated job; live
  ruleset has four Validate contexts and never required `generated`. Refine
  superseded clobber/reuse and `KXM_NPM_PUBLISH`.
  Next: see B2 integration entry for live proof and settings.
- **PR116 tooling** 2026-09-05
  Branch `tooling-headless-harness` after `git merge --no-commit origin/main` (A4 #114 + PR117). Planned by Fable; written by native Grok CLI. Root reviews/PR/CI after this tree.
  Landed: `scripts/harness-run.mjs` fail-closed helper — native auth preflight (`grok models`, `claude auth status`, `codex login status`, `pi auth check --provider openrouter`), verified pairs only, native-provider Pi brake (incl. deepseek), no `impl-pi` Grok fallback, Claude `--safe-mode` + Read/Glob/Grep, Pi JSONL sums every assistant `message_end`, Claude Haiku-first does not steal Fable, private 0600 answer/stderr sidecars, shell:false + win32 `.exe` only. Tests in `test/harness-run.test.ts` (offline fixtures / injected spawn). Docs: AGENTS, D17, Tracking, harness-cli, recipes. Not Phase 11.
  Surprise: Pi `message_end` usage is per call, not assignment-cumulative — must sum two+ assistant events. Scope exceeds the old 600-line guideline because safety tests and docs ride with the helper; not split.
  Next: independent Fable (auth/permissions) + Codex gpt-5.6-sol (CLI/docs) review artifacts; root commit/push/PR. Windows harness dispatch remains unverified. Catalog still lacks grok/Codex authArgs.
- **PR116 tooling rework** 2026-09-05
  Same uncommitted merge on `tooling-headless-harness`. Native Grok writer; no extra model calls.
  Landed: required routing-field preflight; Pi planner/reviewer edit rejected; `max_cost_usd`/`timeout_ms` positive-finite; grok/codex reject hooks/skills including `false`; Pi read-only uses `--no-extensions --no-skills --no-prompt-templates`; justfile user paths via `"$1"` + `JSON.stringify`; `just runs` cost basis labels. A4 updater behavior unchanged.
  Surprise: Claude `--max-budget-usd 0` was accepted then omitted because the argv builder treated 0 as falsy.
  Next: root commit/push/PR after verify. Native review artifacts are not hub peer-reply or human signoff.
- **PR116 tooling test repair** 2026-09-05
  Same uncommitted merge. Native Grok writer; no extra model calls; helper/runtime/justfile unchanged.
  Landed: mandatory just recipe quoting from the justfile body (POSIX `sh` + positional argv; Windows extracted `node -e` / argv, no invalid capture files). Real just binary optional with a visible skip. POSIX shebang/PATH mocks platform-scoped; in-process poll instead of `sleep`. Trailing blank EOF removed. Test-matrix/Tracking/work-log coverage claims clarified without changing phase gates.
  Surprise: the previous just tests required a global binary even though the quoting lives in the recipe line.
  Next: Root Sol checks the new test delta; no re-review of unchanged security helper. Root commit/push/PR.
- **B2 integration** 2026-09-05
  `b2-release-ci` `80b1981` merged `origin/main` `8486973` (PR #116, 7 checks)
  `--no-commit`. Conflicts only CHANGELOG + this work-log; both B2 and tooling
  kept. Proof Tracking stash `9484a79` restored, then wording updated so it
  does not age on merge.
  Landed (docs/settings, not release code): live proof run `33995686608`
  attempts 1/2, same release `383391727` asset `546379172` digest as
  `proof-first.json` / `proof-rerun.json`; temporary draft/tags/branch/worktree
  removed and absence verified; published `v0.5.1` unchanged. Root applied
  `protect-main` `22251971` (four Validate contexts stay, `Plugin validation`
  added); `delete_branch_on_merge` true. First published-asset
  `kxm update --kxm` and public npm/wiki remain later. #119 (Windows queue
  hang) and #120 (POSIX worker PID partial JSON) stay separate PRs.
  Surprise: merge did not retouch release workflow/script/tests beyond main.
  Next: root commit/PR. Do not claim #119/#120 fixed here.
