schema: kxm.agent.v1
purpose: Third provider-distinct critic used by the vNext MOA target.
model:
  profile: critic-gemini
tools:
  preset: read-only
defaultRepositoryAccess: read
repositories:
  control: read
  api: read
  web: read
network: provider-only
resultSchema: kxm.assignment-result.v1
session:
  reuse: compatible-run-scope
